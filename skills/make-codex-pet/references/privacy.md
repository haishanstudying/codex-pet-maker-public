# Runtime privacy boundary

- Plugin source contains no user media or generated pet assets.
- Each run owns its references, prompts, decoded strips, QA, and final package.
- Never use one run as an example or reference for another user.
- Never copy run files into plugin source.
- Image-generation services process references attached for the current run.
- Repository publication is outside this local review workflow.
